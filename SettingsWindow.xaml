<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinUI3Desktop.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="SettingsRoot">
        <NavigationView x:Name="SettingsNavigation"
                        PaneDisplayMode="Left"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        IsPaneToggleButtonVisible="True"
                        IsPaneOpen="True"
                        OpenPaneLength="240"
                        CompactPaneLength="64"
                        PaneTitle="MyDock"
                        SelectionChanged="SettingsNavigation_SelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="AppearanceItem"
                                    Content="外观"
                                    Tag="Appearance"
                                    IsSelected="True">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE771;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="布局" Tag="Layout">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE790;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="交互" Tag="Interaction">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8FB;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="应用" Tag="Apps">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8A7;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="系统" Tag="System">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE770;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.PaneFooter>
                <StackPanel Margin="12,8,12,16" Spacing="2">
                    <TextBlock Text="MyDock"
                               FontSize="12"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="设置中心"
                               FontSize="12"
                               Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                </StackPanel>
            </NavigationView.PaneFooter>

            <Border Padding="48,36,56,48">
                <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                              VerticalScrollBarVisibility="Auto">
                    <StackPanel MaxWidth="820" Spacing="0">
                        <TextBlock Text="设置"
                                   FontSize="32"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,8" />

                        <TextBlock x:Name="PageTitleText"
                                   FontSize="20"
                                   FontWeight="SemiBold"
                                   Margin="0,20,0,6" />

                        <TextBlock x:Name="PageDescriptionText"
                                   FontSize="14"
                                   TextWrapping="Wrap"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Margin="0,0,0,28" />

                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="12"
                                Padding="24">
                            <StackPanel Spacing="8">
                                <FontIcon Glyph="&#xE713;"
                                          FontSize="28"
                                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                                <TextBlock Text="功能将在后续版本中加入"
                                           FontSize="16"
                                           FontWeight="SemiBold" />
                                <TextBlock Text="当前页面仅搭建导航和视觉布局，尚未保存或修改任何应用设置。"
                                           FontSize="14"
                                           TextWrapping="Wrap"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </NavigationView>
    </Grid>
</Window>
